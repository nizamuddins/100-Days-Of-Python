# generating random numbers
import  random

# generating random integers

random_num = random.randint(1,10);

print(random_num)

# generating random floating numbers

random_float = int(random.random()*10)+1;

print(random_float);
